Salut <%= @user.first_name %> !
==========================================================

Tu as reçun une nouvelle commande pour les cartes suivante:
<% @item.each do |item| %>
- <%= item.image_url%> <%= item.title %>

Pour accéder à ton espace admin, connecte-toi via : <%= link_to "e-commerce", root_path %>.

À très vite !
